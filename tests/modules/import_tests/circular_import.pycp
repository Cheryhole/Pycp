// 循环导入（真正互相 import）场景：ca import cb，cb import ca。
// 已知行为：业务代码可成功执行（双方顶层符号可用），但进程退出阶段
// 会段错误（模块缓存互相引用，GC 释放时崩溃）。故本用例被 runner 标记
// 为 EXPECTED-FAIL（预期非零退出），用于如实记录该边界行为。
import io
import Pycp
import mod_circ_x
import mod_circ_y

func main() {
	io.print(mod_circ_x.X_VAL)
	io.print(mod_circ_y.Y_VAL)
	io.print("circular_import reached main (business logic ran)")
}

main()
