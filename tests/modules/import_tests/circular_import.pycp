// 循环导入（真正互相 import）场景：ca import cb，cb import ca。
// 现已正常：双方顶层符号可用，且进程干净退出（模块缓存循环引用被 GC 正确
// 打破，不再段错误）。作为正常 PASS 用例回归。
import io
import pycp
import mod_circ_x
import mod_circ_y

func main() {
	io.print(mod_circ_x.X_VAL)
	io.print(mod_circ_y.Y_VAL)
	io.print("IMPORT TEST PASS: circular_import")
}

main()
