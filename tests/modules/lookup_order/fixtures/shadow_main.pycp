// 入口：同名模块同时存在于 exe 目录 stdlib/（第 2 层）与 cwd（第 3 层），
// 验证第 2 层优先——期望取到 stdlib 版本的取值 1。
import io
import Pycp
import shadow

func main() {
	o = shadow.shadow_origin
	if (o != 1) {
		io.print("mismatch: got " + Pycp.String(o))
		return None
	}
	io.print("LOOKUP PASS: stdlib over cwd")
}

main()
